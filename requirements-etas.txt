# Optional dependency: the independent third-party ETAS reference implementation
# (lmizrahi/etas), used only to invert an independent parameter set for the
# `modern_etas` comparator (marmara.etas_modern integrates its kernels; the fit is
# run once against this environment).
#
# Install into a separate virtual environment — this package pulls numpy>=2.5 /
# pandas>=3.0 / scipy>=1.18, which conflict with the core requirements.txt pins:
#
#   python -m venv .venv-extra
#   .venv-extra/bin/pip install -r requirements-etas.txt
#
# Pinned to the exact commit used to produce results/etas_mizrahi_fit.json.
etas @ git+https://github.com/lmizrahi/etas@097f08b69a4f06f9c38d14799dedfbd4543144e3
