# Optional dependency: pyCSEP — the community CSEP consistency-testing toolkit, used
# by scripts/csep_run.py. Install into a separate virtual environment (it pulls
# cartopy / obspy / rasterio and its dependencies conflict with the core pins):
#
#   python -m venv .venv-extra
#   .venv-extra/bin/pip install -r requirements-csep.txt
#
# marmara.csep_eval also implements the same Savran et al. (2020) catalog-based
# consistency tests (N / M / S / pseudo-likelihood) in the core environment, as a
# cross-check on the pyCSEP numbers.
pycsep
